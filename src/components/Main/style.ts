'use client'

import styled from "styled-components";

export const Capsule = styled.main`
    width: 100%;
`;

export const Main_wrapper = styled.main`
    width: 100%;
    display: flex;
    flex-direction: column;
`;

export const Main_top_img = styled.img`
    margin-top: 0vh;
`;